package db

import (
	"log"

	"gorm.io/gorm"
)

// SeedDatabase seeds the database with initial data
// Currently a placeholder as you mentioned no actual seeding is needed
func SeedDatabase(db *gorm.DB) error {
	log.Println("Database seeding functionality is prepared but not currently implemented")

	// Add your seeding logic here when needed

	return nil
}
